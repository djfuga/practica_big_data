# =============================================================
# Practica Big Data 2026 - Parte II
# Stack docker-compose
#
# Levantar:  docker compose up -d
# Apagar:    docker compose down
# Logs:      docker compose logs -f <servicio>
# =============================================================

name: bigdata-practica

# -------------------------------------------------------------
# Red compartida: todos los servicios se ven por su nombre DNS
# Ejemplo: el Spark master ve a MinIO como http://minio:9000
# -------------------------------------------------------------
networks:
  bigdata-net:
    driver: bridge
    name: bigdata-net

# -------------------------------------------------------------
# Volumenes persistentes (sobreviven a docker compose down)
# Para borrar TODO: docker compose down -v
# -------------------------------------------------------------
volumes:
  minio-data:
    name: bigdata-minio-data

services:

  # ===========================================================
  # MinIO - Object Storage S3-compatible
  # Base del Data Lakehouse (Iceberg + parquet)
  # ===========================================================
  minio:
    image: minio/minio:RELEASE.2025-04-08T15-41-24Z
    container_name: bigdata-minio
    restart: unless-stopped
    networks:
      - bigdata-net
    ports:
      # 9000: API S3 (la que usan Spark, boto3, mc)
      - "${MINIO_API_PORT:-9000}:9000"
      # 9001: Web console (UI grafica)
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_REGION: ${MINIO_REGION:-us-east-1}
      # MinIO usa por defecto el navegador del puerto 9001
      MINIO_BROWSER_REDIRECT_URL: http://localhost:${MINIO_CONSOLE_PORT:-9001}
    volumes:
      - minio-data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      # /minio/health/live es el endpoint nativo de MinIO
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ===========================================================
  # minio-init - Job de un solo uso: crea buckets y usuarios
  # Se ejecuta DESPUES de que MinIO este healthy y termina
  # ===========================================================
  minio-init:
    image: minio/mc:RELEASE.2025-04-03T17-07-56Z
    container_name: bigdata-minio-init
    networks:
      - bigdata-net
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_APP_USER: ${MINIO_APP_USER}
      MINIO_APP_PASSWORD: ${MINIO_APP_PASSWORD}
      MINIO_LAKEHOUSE_BUCKET: ${MINIO_LAKEHOUSE_BUCKET}
    volumes:
      - ./bootstrap/minio-init.sh:/init.sh:ro
    # /bin/sh porque mc usa Alpine y no tiene bash
    entrypoint: ["/bin/sh", "/init.sh"]
